
## Session 02:15

### 02:15
<!-- session:4033bf67-01a9-4823-b311-7e4a9b1deed0 turn:42da5ac3-4ea0-4630-84dd-1a19cd8bd184 transcript:/home/lurui/.claude/projects/-home-lurui-workspace-R4Det/4033bf67-01a9-4823-b311-7e4a9b1deed0.jsonl -->
- 用户要求按照之前的 TODO 列表汇报工作进度，确认完成情况、是否有额外事项以及下一步计划。
- TODO 1（TJ4D_dataset.py）已完成：__init__ 添加 seq_len 参数、__getitem__ 取 N 帧并基于连续性判 per-frame valid、_pack_final_data 重写为接收 N 帧列表、_create_empty_prev_data 重命名为 _create_empty_frame_data。
- TODO 2（R4Det.py）已完成：__init__ 添加 seq_len、forward_train/simple_test 循环化 extract_feat，历史帧使用 no_grad 循环，当前帧保留梯度。额外发现并修复了一个 bug：当前帧 extract_feat 的 is_valid_mask 应传上一帧 validity，改为传 last_hist_valid。
- TODO 3（rssm_fusion.py 的 reset_for_samples）发生偏离：原计划从清零改为跳过，但实际决定保留清零语义。理由是 per-frame validity 从当前帧往回追连续性，中间帧不会无效，清零和跳过在 N=2/N=3 下等价，且清零更简单并能精确复现现状。已更新 plan 文档说明但 TODO 文字未改。
- TODO 4（config RSSM 加 seq_len=3）已完成：model、train/val/test dataset 均已添加，config load 验证通过。
- TODO 5（N=2 回归验证复现现状不掉点）尚未执行。
- TODO 6（N=3 单 iter 冒烟）进行中：编写了 tools/smoke_seq3.py 脚本，数据侧已验证正确（__getitem__ 产出 img shape 正确、metas/points 长度正确、frame_valid 正确、collate 后 batch 结构匹配 forward_train 预期），模型构建遇到 meta_info 缺失已修复，正要跑 forward_train 时被打断。
- 事后确认无超出 TODO 范围的新事项，TODO 3 的相反决定属于实现细节，下一步计划是继续跑 smoke_seq3.py 的 forward_train 检查 loss/shape/显存。

### 09:54
<!-- session:4033bf67-01a9-4823-b311-7e4a9b1deed0 turn:383ac528-4e86-4311-9d58-05c96d3b9a28 transcript:/home/lurui/.claude/projects/-home-lurui-workspace-R4Det/4033bf67-01a9-4823-b311-7e4a9b1deed0.jsonl -->
- User requested to continue running tests, noted that CUDA_VISIBLE_DEVICES environment variable was already set, asked to check which GPUs are free before testing, and asked why N=2 regression was not being performed.
- GPU status was checked: GPUs 0-3 were idle (5MB usage, 0% utilization), while GPUs 4-7 were running training jobs. GPU 0 was selected for testing.
- Encountered a scatter error where `device` was passed as a string `'cuda:0'` instead of an integer index; this was fixed by passing `[0]` as the device argument.
- Hit a gt_bboxes_3d unpacking error: in the smoke test, `cur()` was prematurely extracting current-frame GT before passing to `forward_train`, causing an index-out-of-bounds error when `forward_train` tried to do `gt[N-1]` on already-extracted single-frame box objects.
- Fixed the smoke test by removing the premature `cur()` call, letting `forward_train` handle frame extraction internally.
- Discovered that historical frame `img_metas` were missing `gt_bboxes_3d`/`gt_labels_3d`, which `preprocessing_information` reads during training. Original code filled GT for `prev_img_metas` but Claude Code's refactored loop only filled GT for current frame metas.
- Refactored `forward_train` to fill per-frame GT into each frame's `img_metas`, then separately extract current-frame GT for loss computation.
- N=3 smoke test passed: `img shape (2,3,3,480,640)` correct, `loss_rssm_kl=9.60` at initialization (comparable to rssm3 epoch 1 KL of 7.66), all detection losses present and finite, about 62% of elements exceeded free_nats.
- N=2 smoke test also passed: `img shape (2,2,3,480,640)` correct, confirming same code path works for both sequence lengths.
- User had asked why N=2 regression matters; Claude Code explained that N=2 regression is the safety net to verify that loopification changes don't introduce behavioral deviations, isolating the variable before jumping to N=3 training.
- Claude Code recommended running N=2 regression for 1-2 epochs on GPUs 0-3 before proceeding to full N=3 training, and asked if User wanted to start immediately or review the current diff first.

